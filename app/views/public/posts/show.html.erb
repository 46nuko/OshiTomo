<div>
  <%= image_tag @post.image %>
  <p><%= @post.body %></p>
  <p>ユーザーネーム：<%= @post.user.name %></p>
  <p>投稿日：<%= @post.created_at.strftime('%Y/%m/%d') %></p>
  <% if @post.user == Current.user %>
    <%= link_to "削除", post_path(@post_image), data: { turbo_method: :delete } %>
  <% end %>
</div>